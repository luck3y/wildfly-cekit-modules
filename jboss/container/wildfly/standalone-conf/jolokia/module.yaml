schema_version: 1
name: jboss.container.wildfly.standalone-conf.jolokia
version: '1.0'
description: jolokia enabled in standalone.conf 

execute:
- script: configure.sh
  user: '185'

modules:
  install:
  - name: jboss.container.jolokia.bash

artifacts:
 -name: jolokia-jvm-1.6.2.redhat-00002-agent.jar
  url: https://repo1.maven.org/maven2/org/jolokia/jolokia-jvm/1.6.2/jolokia-jvm-1.6.2-agent.jar
  md5: d27b2fc0d1ed8ed50a7e661db839c83a

