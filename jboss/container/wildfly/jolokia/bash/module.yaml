schema_version: 1

name: jboss.container.wildfly.jolokia.bash
version: '1.0'
description: ^
  Provides support for configuring community version of Jolokia.

modules:
  install:
  - name: jboss.container.jolokia.bash
